# Umbrella Skill Library

This directory stores procedural skill packs used by Umbrella.

Layout:

- `<slug>/SKILL.md` - skill metadata + steps
- `<slug>/scripts/` - optional helper scripts
- `<slug>/examples/` - optional examples

The loader reads only `SKILL.md`; supplementary files are optional.
