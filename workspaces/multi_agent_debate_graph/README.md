# Multi-Agent Debate Graph (gMAS)

Постановка: `TASK_MAIN.md`.

## Установка и тесты

```bash
uv sync
uv run python -m pytest tests -q
```

Зависимость `frontier-ai-gmas`: см. `[tool.uv.sources]` в `pyproject.toml`.
